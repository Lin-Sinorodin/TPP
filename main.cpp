#include "calculator.h"


int main() {
    calculatePrint(15, '+', 7);
    calculatePrint(10, '-', 80);
    calculatePrint(71, '*', 49);
    calculatePrint(49, '/', 71);
    calculatePrint(15, '@', 7);
    calculatePrint(15, '/', 0);
    return 0;
}
